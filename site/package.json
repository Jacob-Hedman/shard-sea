{
  "name": "dnd35-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "export": "node --no-warnings scripts/export-data.mjs",
    "auth:dev": "wrangler pages dev",
    "deploy": "astro build && wrangler pages deploy",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.6.0"
  }
}
